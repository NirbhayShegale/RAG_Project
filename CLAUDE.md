**CRITICAL TOOL INSTRUCTION:**
When calling tools, you MUST use the exact TitleCase capitalization provided in the tool schema (e.g., you must output `Read`, `Bash`, `Glob`, `View` exactly as written). Do not convert tool names to lowercase, or the system will crash.

You are running on a Windows machine. Do not use Unix-specific shell commands. Rely on your native tools for file exploration, or use standard PowerShell syntax when `Bash` execution is necessary.